# Copy to .env.local for local dev (Vite reads .env.local automatically).
# VITE_* variables are baked into the bundle at build time.

# URL the browser uses to reach the API.
VITE_API_URL=http://localhost:3001
